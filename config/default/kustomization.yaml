apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- namespace.yaml
- ../crd
- ../rbac
- ../manager
- secret.yaml

patchesStrategicMerge:
- manager_auth_proxy_patch.yaml

images:
- name: controller
  newName: otomato/khook
  newTag: latest

namespace: kagent-system